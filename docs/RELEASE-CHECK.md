# 发布前全面自测（第一个正式签名版）

> 日期：2026-08-19 ｜ 基线：`8a41f46`（品牌视觉二期收官）＋ 本轮修复
> 背景：Apple Developer 证书已到，即将打第一个正式签名版给真实客户
> 性质：**只查不改 + 高严重度当场修**。全部结论以实测为准，未跑到的一律标「未覆盖」，不写"应该没问题"
> 真实调用预算：批准 ¥5，实花见 §5

## 严重度定义

| 级别 | 含义 |
|---|---|
| 🔴 **阻断发布** | 会泄漏数据 / 让客户机进入不可用状态 / 违背产品对外承诺 |
| 🟠 **发布后尽快修** | 客户会遇到、会误判，但能绕开或影响面可控 |
| ⚪ **记账不阻断** | 打磨项、开发者侧问题、或已知边界 |

---

## 0. 结论摘要

| # | 问题 | 严重度 | 状态 |
|---|---|---|---|
| R-01 | 敏感文件上云：`sensitive` 判据只看前 800 字符，A-3 之后被长 frontmatter 顶出窗口 | 🔴 阻断 | **已修 + 已加断言** |
| R-02 | 云端已存在敏感内容存量 | 🔴 阻断 | **✅ 两个账号均已清理并验证**；33 条 MOC 存量作为显式接受的残留风险记账（§6c） |
| R-03 | MOC/主题索引不带敏感标记，却逐条摘录了敏感文件的人名/职级/合同状态，正常上云 | 🟠 | **已修**（pkb-pipeline `bf07070`，冻结并同步提交） |
| R-04 | 更名连带面：`productName` 与界面名不一致，且签名单要不要一起改 | 🟠 决策项 | 见 §2.3，**结论与 HANDOFF §4-27 的记载不同** |
| R-05 | 离线条不会自己下去：`probeCloud` 无周期重探 | 🟠 | 见 §2.4 |
| R-06 | `usage-report.mjs` 把「无 route 记录数」报成 6 倍 | ⚪ | **已修** |
| R-07 | 走查旧名扫描把测试账号邮箱/走查目录当成旧品牌名（假红） | ⚪ 测试侧 | **已修** |
| R-08 | `render_pptx`/`render_document` 的临时 spec JSON 从不清理 | ⚪ | 见 §2.8 |
| R-09 | 生成产物那一轮的折叠摘要说「未找到相关资料」，旁边却摆着刚生成的文档 | 🟠 | 见 §3 |
| R-10 | 升级不会把老用户从中转站挪回官方直连（成本 2.7×），需人工改 | 🟠 决策项 | 见 §2.2 |
| R-11 | 建库引导整屏没有任何品牌标识 | ⚪ | 见 §2.1 |
| R-12 | 空库时 chips 仍写「检索我的库」 | ⚪ | R-3 顺带发现，见 §6b |

> **全部裁决（2026-08-19）已落地并重验**：三条见 §6b，R-02 / R-03 / 存量双卡见 §6c。
> **§9 已定稿为「可发布」。**

---

## 1. 全量测试体系（当前 HEAD）

| 项 | 结果 | 说明 |
|---|---|---|
| `npm run typecheck` | ✅ | 两个 project 均无错 |
| `npm run build` | ✅ | |
| `node e2e/walkthrough.mjs`（本地模式） | ✅ | 102 张截图刷新，截图基线无残留 |
| `node e2e/a1-enqueue.mjs` | ✅ | 96/96 入箱、相对路径逐条相同、落位一致率 **100%**（命中 92 / 不一致 0） |
| `npm run smoke:steps` | ✅ | |
| `npm run smoke:cards` | ✅ | **本轮新增 7 条 A-8 断言**（见 R-01） |
| `npm run smoke:resume` | ✅ | |
| `node e2e/login-provision.mjs` | ✅ | 登录 827ms、两把 key 自动下发、零配置对话 3s 成功 |
| `E2E_CHAT=1 node e2e/walkthrough.mjs` | 见 §1.1 | |
| `npm run smoke:provider`（最小集） | 见 §1.2 | |

### 1.1 E2E_CHAT 全量轮

✅ **跑到终点，零红**（第三轮；前两轮红在走查自己的旧名扫描上，见 R-07）。

- **刷新 119 张截图**，其中包含 HANDOFF 里那批「仍是品牌二期之前旧配色」的 E2E_CHAT 专属基线
  （01d/01d3/01e/28/35/36/39/41c-e/46/48–53），**本轮已自然刷新，该条已知状态可以销账**
- 截图基线干净，无无人认领的残留
- 关键断言逐条过：登录即用 / M-29 重复下发零写入 / 步骤流参数层 / 验证性扫描 / 产物时长两形态 /
  失败步骤 / 会话恢复降级 / H-09 停止留半截 / H-10 拒绝重发 / M-11 三个出错分支 /
  编辑冲突三选一 / syncQueue 退避 / 投递取消无残留 / before-quit 无孤儿 / 用量写入链路
- **一条软警告（不是红）**：`检索策略（第一问）耗时 128.8s，达标60秒=false`。
  拆开看是 `检索次数 1 / 文件查找放行 4 / 被护栏拒绝 0 / 猜测 0` —— **没有瞎试**，
  时间花在"读原始大表 + 汇总"上。同 HANDOFF §3-13：根在云端语义检索给不出可引用出处，
  与本轮改动无关

### 1.2 smoke:provider 最小集

✅ **DeepSeek 官方（标准档出厂映射）3/3 通过**

| 用例 | 结果 |
|---|---|
| 单轮对话 + 流式 + 模型钉死 | ✅ 12.7s，`modelUsage` = `deepseek-v4-flash` / `deepseek-v4-pro`（主模型在里面 → 没被静默换掉） |
| abort 可停止 | ✅ 5.4s，停在第 3 个 delta |
| 工具调用 `search_knowledge` | ✅ 19.5s，真调到 3 次 |

**增强档（aihubmix / `claude-opus-5`）真实调用本轮未跑**——单轮实测 ≈¥7（HANDOFF「已知未验项」：增强 2 次 ¥13.98），一次就超掉整轮预算；且 §3-6 记着这把 key 被打穿过余额，打穿即桌面版 + 网页版一起挂。改为本地断言覆盖（置灰分支 / 回落逻辑 / 切标准重试出口），**真实调用属未覆盖项**。

---

## 2. 发布前专项检查

### 2.1 全新装机路径

新脚本 `e2e/fresh-install.mjs`，走的是**真实客户第一天**那条次序，✅ **全绿**。
与主走查的分工：主走查跑在预先铺好的库上、验各个交互；这条验**从零到有的次序本身**。

| 步 | 断言 | 结果 |
|---|---|---|
| 1 冷启动 | 全新 userData 无 key、无库路径，落登录门 | ✅ 截图 `60` |
| 2 登录 | 两把 key 自动下发 | ✅ 截图 `60b` |
| 3 建库向导 | **新建模板库**分支，`layout.json` 有 `entities` 段 | ✅ 截图 `61`/`61b` |
| 4 整包拖入 | 7 个文件（含两级子目录）递归收全 | ✅ |
| 5 落位 / 实体卡 / 双链 | 无「未分类」、实体卡建起来、**正文双链 ≠ 0** | ✅ 截图 `62` |
| 6 A-8 边界 | 盘上 3 篇敏感，`cloudSync` 拦下 **3 篇**，数对得上 | ✅ |
| 6b A-4 边界 | 损坏文件进 `.failed/`，界面报出失败计数 | ✅ |
| 7 问答 | 真实调用拿到带引用的回答 | ✅ 截图 `63` |
| 8 产物 + 入库 | 生成 Word → 入库成功 | ✅ 截图 `64`/`64b` |

**这条路以前是零 e2e 覆盖的**——「新建模板库」要弹系统保存框，Playwright 驱动不了，
所以本轮加了走查开关 `MCNAI_E2E_NEW_VAULT`（判据同 §4-22：真触发它需要驱动原生对话框）。
而 A-3 那个「双链 352 → 2」的 bug 恰恰就长在这条路上，现在有哨兵了
（哨兵**刻意排除 MOC/主题索引**：那两篇天生一堆双链，算进来这条断言永远绿）。

**实测结果**：模板库跑完 15 节点 / 31 边，达人（灰太太）、产品（pink bear / 丰唇蜜）、
合作方（霞飞）三类实体卡都建起来了，A-3 在这条路径上成立。

**咯噔一处 → R-11**：建库引导那一屏**整屏没有任何品牌标识**（登录门有 72px logo，
这一屏什么都没有）。新客户登录后看到的第一屏是一块无名白板。⚪ 打磨项。

### 2.2 升级路径（老 userData 覆盖安装）

新脚本 `e2e/upgrade-path.mjs`，✅ **全绿**（截图 `54`）。
做法：把**本机真实 userData 整份拷进隔离目录**再让新构建接管——这就是"覆盖安装"的等价形态
（拷贝不动原件；库路径改指一个自造的空库，免得测试实例的 watcher 盯上客户数据）。

本机真实形态：24 个配置字段 / 1 条会话 / `tierMigrated=true` / 标准档 `api.inferera.com` / `pricing.rev=3`。

| 断言 | 结果 |
|---|---|
| 10 个配置字段逐个无损，没被出厂默认覆盖 | ✅ |
| 落盘的 vaultPath 被沿用，不掉进建库向导 | ✅ |
| 会话历史条数不变 | ✅ |
| 两把 key 都还在（safeStorage 跨版本读得回来） | ✅ |
| 登录态恢复，不要求重新登录 | ✅ |
| `tierMigrated=true` → **不再二次迁移**，档位映射原样保留 | ✅ |
| `pricing.rev` 不倒退 | ✅ |

**顺带查实一条与发布相关的事 → R-10**：正因为"不再二次迁移"是对的，
**升级不会把老用户从中转站挪回官方直连**。本机（形态与大头那台一致）的标准档仍指着
`api.inferera.com`，而 HANDOFF §0 待办 6 记着这条线路成本是官方直连的 **2.7 倍**。
发版本身修不了它，得**在管理员区手动把标准档 baseUrl 改成 `https://api.deepseek.com/anthropic`**。

### 2.3 更名连带影响面

**先纠正一处事实**：`productName` **并没有**改成 SamePage。

```
electron-builder.yml:2      productName: mcn-ai        ← 未改
desktop/package.json name   mcn-ai-desktop             ← 未改
src/main/index.ts:28        title: 'SamePage'          ← 只有界面字符串改了
```

这是 HANDOFF §4-27 拍板过的：更名只改"看得见的"。**但那条决策记的触发条件正是「买到 Apple Developer ID、做签名与公证的那一单」——也就是你现在这一单。**

#### 实测：改 `productName` 到底会连带什么

§4-27 给的理由是「改 `productName` 会让 `app.getPath('userData')` 跟着变 → 所有已装机器强制重新登录」。**这条理由对本仓库不成立**。

**直接证据**（新脚本 `e2e/probe-name.mjs`，跑在**打包形态**的 app 上，读的是主进程里的真实值）：

```json
{
  "形态": "打包",
  "app.getName()": "mcn-ai-desktop",
  "默认 userData（= appData/getName）": "/Users/…/Library/Application Support/mcn-ai-desktop",
  "Keychain 服务名（推导）": "mcn-ai-desktop Safe Storage",
  "safeStorage 可用": true
}
```

与磁盘上的实际情况逐项对得上：

| 观测项 | 实际值 | 跟谁走 |
|---|---|---|
| `app.asar` 内 `package.json` 的 `name` | `mcn-ai-desktop` | — |
| `app.asar` 内 `package.json` 的 `productName` | **无此字段** | — |
| `Info.plist` 的 `CFBundleName` | `mcn-ai` | electron-builder 的 `productName` |
| 实际 userData 目录 | `~/Library/Application Support/**mcn-ai-desktop**` | **package.json `name`** |
| 实际 Keychain 条目 | `**mcn-ai-desktop** Safe Storage` | **package.json `name`** |

两个"会丢数据"的东西（userData 目录、Keychain 服务名）跟的都是 **package.json 的 `name`**，不是 electron-builder 的 `productName`——因为打包进 asar 的 `package.json` 里**根本没有 `productName` 字段**，`app.getName()` 于是落到 `name` 上。

**结论**：
- 只改 `electron-builder.yml` 的 `productName` → 改变 .app 包名、dmg 名、Finder/Dock 显示名、`CFBundleName`；**userData 与 Keychain 不动，老用户不会被强制重登**
- 真正会导致强制重登的是改 `package.json` 的 `name`，或给 `package.json` 加 `productName` 字段——**这两件事不要做**

#### 当前的名字一致性

| 出现位置 | 现在显示 | 证据 |
|---|---|---|
| 界面（侧栏字标 / 登录页 / 窗口标题 / 菜单项文案 / 诊断报告） | **SamePage** | 走查旧名扫描五个页面各扫一次，零命中 |
| Finder / 应用程序文件夹 / Dock | **mcn-ai** | `lsappinfo` 实测 `LSDisplayName="mcn-ai"` |
| dmg 文件名 | **mcn-ai-0.1.0-arm64.dmg** | 本轮 `npm run dist` 产出 |
| macOS 菜单栏最左那个应用名 | 由 `CFBundleName` 决定 = **mcn-ai** | 同 `LSDisplayName`；**这一条我没能亲眼截到**（读菜单栏要辅助功能授权，本机未授权），按 Electron 的既有行为推的，**发版前请人工瞄一眼** |

客户装完之后，Dock 上是 `mcn-ai`、点开界面写 SamePage。**这是发客户前最好一起收掉的不一致**，而且按上面的实测，收掉它的代价比 §4-27 记的小得多。

#### 裁决（2026-08-19，用户拍板）→ **同日晚推翻，已改为 SamePage**

> **最终结论：`productName: SamePage` 已落地。** 下面这段是当天早些时候的裁决，
> 保留是为了说明"为什么当时不改、后来又为什么改"。
> 推翻的理由：名字不一致是客户直接看得见的东西，值得现在收掉；
> 而上面那张实测表已经证明改它不动 userData、不动 Keychain。
> 改名后复验：`CFBundleName`=SamePage 而 `app.getName()` 仍是 `mcn-ai-desktop`，
> `upgrade-path.mjs` 七件事全绿、会话真解密、不要求重登。
> **唯一实操代价**：旧的 `mcn-ai.app` 与新的 `SamePage.app` 是两个文件，
> 覆盖安装不成立，装机必须手动删旧 app（`docs/RELEASE.md` §2）。

~~**`productName` 不改**，维持内部名，显示层 SamePage 已足够。~~
dmg 文件名 / .app 包名 / Finder·Dock 名 / 菜单栏应用名**暂时继续是 `mcn-ai`**，
这条不一致**记账不阻断发布**，已写进 HANDOFF §4-27。
**真正的内部改名等"天然要求重登"的时机搭车**——当前最可能的是网关切换那一单。

> 记这条的理由：上面那张实测表说明"改它其实很便宜"，很容易在将来某一轮被人当成
> 顺手改掉的小事。**它便宜不等于现在该改**——第一个正式签名版上少一个变量是更值钱的。

### 2.4 离线与弱网

| 项 | 结果 |
|---|---|
| 断网启动 → 照常开窗 + 本地功能可用 + 降级条 | ✅ 主走查 33：`云端离线，本地功能照常可用（知识库 / 投递箱）；AI 检索已降级为本地全文`，且知识库真能打开 |
| 登录超时分类提示 | ✅ 三类都验到：超时 10.4s「登录超时（10 秒没有响应）…」/ 网络「连不上服务器…可先『暂不登录，仅本地使用』」/ 可取消 26ms 回到可操作 |
| **恢复网络后自动回复** | ❌ **不成立 → R-05** |

R-05 是新脚本 `e2e/offline-recovery.mjs` 实测出来的：黑洞地址冷启动 → 离线条出现 →
把地址改回真地址（= 网络恢复）→ **纯等 90 秒，条子纹丝不动**；
对照组重启一次，条子立刻下去 —— 说明恢复链路本身是好的，**只是不会自己好**。

> 这个脚本在 R-05 修好之前是**故意红的**，它就是这条缺口的凭证。

### 2.5 敏感边界

| 项 | 结果 |
|---|---|
| 敏感文件不上云 | 🔴 **修前会漏 4 篇** → R-01（已修，647 篇对照 0 不一致） |
| 拦截计数与盘上敏感篇数一致 | ✅ 新装路径实测 3 vs 3 |
| 实体卡敏感继承 | ✅ 175 张里 114 张正确继承；**非敏感卡引用敏感文档 0 处** |
| 三态开关行为 | ✅ 界面三档、存储两个独立布尔、默认 `local`；文案说清真实边界；「仅影响之后入库的文件」有明说 |
| MOC 会不会带出敏感内容 | 🟠 **会** → R-03 |

### 2.6 用量与计价

✅ **用量页与 jsonl 对账通过**（走查库 15 条记录，独立重算）：

| 口径 | 用量页 | jsonl 独立算 |
|---|---|---|
| 本月对话次数 | 8 | 8 ✅ |
| 产物数 | 3 | `make-ppt` 1 + `make-docx` 2 = 3 ✅ |
| 增强档 tokens | 输入 22,000 / 缓存读 100,000 / 输出 4,000 | 一致 ✅ |
| 增强档估算花费 | ¥1.87 | 手算 `(22000×5 + 100000×5×0.1 + 4000×25)/1e6 = $0.26 × 7.2 = ¥1.87` ✅ |
| 14 天分档柱状图 | 混合柱 13 = 标准 12 + 增强 1 | ✅（断言量的是像素高度不是数 div） |
| 降级次数 | 0 | 0 ✅ |
| 金额默认隐藏（`showCost`） | ✅ 默认无金额，管理员区打开后才出 ¥ | |

**一处口径差要说清楚（不是 bug）**：产品算 token 取的是 `modelUsage`（按模型汇总，含轻量模型子调用），
比顶层 `usage` 每轮多约 430 token。**产品这边更全**，符合 §4-25「归一化放汇总侧」的设计。

**已知缺口仍然成立**：4 条 `ingest-tag` 记录 token 全 0 —— 入库打标的花费完全不入账
（HANDOFF §3 bug#8，本轮未修，页面已有「上面的花费不含入库打标」的说明）。

### 2.7 错误路径抽样

| 类 | 触发方式 | 结果 |
|---|---|---|
| AI 出错重试（预检失败） | 切到没配 key 的线路 | ✅ 气泡内「重试」、提问不被复制成两条 |
| AI 出错重试（异步 401） | 秒回 401 的本地 http 桩 | ✅ 提问仍在、重试不堆叠、端点恢复后重试真的成功 |
| 一次失败只出一条中文气泡（T-02） | 同上 | ✅ `⚠️ AI 服务的密钥无效或已过期，请在设置里重新获取服务端配置`，无英文原文 |
| 会话恢复降级 | 伪造过期 sdkSessionId | ✅ 不报错、上下文真接上（记 4271 再问答得出） |
| 投递失败 `.failed` | 塞一个内容是垃圾字节的 `.docx` | ✅ 进 `.failed/20260819/`，界面报出失败计数（A-4 成立） |
| toast 各语义形态 | 走查连点触发 | ✅ 6 个样本、4 种语义（warn/info/ok/error）、**统一炭黑底** `rgb(61,57,47)`、宽度自适应上限 420px、圆角 12 / 字号 13 / 内边距 10·16 全一致 |
| 增强档失败的出口 | 增强档没 key | ✅ 「切换到标准模式重试」真点一次，切回标准且不再报错 |

**抽样时抓到一条 → R-09**：生成产物那一轮的折叠摘要写「核对完成，未找到相关资料」，
而右边产物面板里躺着刚生成的 Word（截图 `64b` 是现场证据）。

### 2.8 资源与残留

| 项 | 结果 |
|---|---|
| 日志大小 | ✅ `lib/logger.ts` 2MB 滚一档，上限 `main.log` + `main.log.1` ≈ 4MB。本机实测 1.25MB + 2.0MB |
| 临时目录 | 🟠 `render_pptx` / `render_document` 每次生成都往 `os.tmpdir()` 写一个 spec JSON（`mcnai-spec-*.json` / `mcnai-doc-*.json`）且**从不删除**。本机实测残留 25 个。落点是 macOS 的 per-user 私有目录（权限 700，系统会定期清），所以**不是泄漏，是残留**——但 spec 里带库内正文，值得顺手清掉 |
| 退出无孤儿进程 | ✅ **打包形态实测**（`MCNAI_APP_BIN=release/mac-arm64/mcn-ai.app/…` 跑整轮主走查，零红）：<br>· H-13「停止本轮」→ `ps -eo pgid` 查该进程组 **无残留**，且**不回滚**（md 549 → 549 篇一篇不少）<br>· `before-quit` → 退出前 pipeline 进程组存活、退出后 **无孤儿**<br>· 收尾再全局扫一次 `mcn-ingest` / Electron：**零残留** |
| 日志增长速率 | ✅ 跑完一整轮走查后实例日志仅 **1.2KB**——按这个速率，2MB 的滚动阈值够用很久 |

> 「取消」与「before-quit」按 HANDOFF §2-14 必须在**打包形态**下回归（dev 形态验过不算数），
> 本轮已在本次 `npm run dist` 出的新构建上跑过。

---

## 3. 问题清单（按严重度）

### 🔴 R-01 敏感文件会上云（已修）

**现象**：`cloudSync` 判定一篇笔记是不是敏感时，只读文件**前 800 字符**再跑正则。

**根因**：A-3 上线后 frontmatter 会带 `entities_talent` / `entities_product` / `entities_partner` 长数组，而 `sensitive: true` 是 `03b`/`09` **最后**写的一行——被切在窗口之外。

**实测**（Maggie 全量库，`/tmp/mcnai-a1-vault`）：

| 文件 | `sensitive:` 行落在第几字符 | 修前 | 修后 |
|---|---|---|---|
| 签约-2026年度目标管理总表.md | 1673 | 判为非敏感 → 上云 | 拦住 |
| 【带货MCN】达人信息表.md | 1569 | 判为非敏感 → 上云 | 拦住 |
| 2026年收支利润表【收付实现制】.md | 1168 | 判为非敏感 → 上云 | 拦住 |
| OMG美妆x向日花年框合作.md | 1039 | 判为非敏感 → 上云 | 拦住 |

**为什么以前没发现**：走查库（`/tmp/mcnai-e2e-vault`）的 frontmatter 最长 313 字符，够不着这个窗口；A-8 那次「37 篇拦截」的验证做在 A-3 **之前**；A-3 收官时 `cloudSync` 只改了「判敏感读盘不读内存索引」，没有人量过 800 够不够。

**修法**：`src/main/lib/sensitive.ts` 新模块，判据改为**整块 frontmatter 都看**。
顺带说明：旧写法根本没省 I/O——`readFile` 本来就把整份读进内存了，`slice(0,800)` 只是少跑一段正则。

**验证**：
- 对两个真实库共 **647 篇**笔记跑对照：与地面真相 **0 不一致**，4 篇原本漏掉的全部拦住
- 反例不误伤：无 frontmatter / 正文里有同名行 / `sensitive: false` / 空文件
- 失败取向：frontmatter 开了头却不闭合 → **按敏感处理**（宁可少传一篇，不可误传一篇）
- **加了永久断言**：`npm run smoke:cards` 新增 7 条（零 token），含 >800 与 >1.5 万字符的长 frontmatter 形态。这个洞能溜进来就是因为判据长在闭包里、没有任何断言守它

**影响面自查（都干净）**：
- 实体卡：175 张里 114 张正确继承敏感；**非敏感卡引用敏感文档 0 处**
- 全仓只有这一处 `sensitive` 判定点，无同型第二处

### 🔴 R-02 云端已有敏感内容存量

> **已裁决并部分执行，处置结果见 §6c**：测试账号已清空并验证；大头账号已查证出清单、只查未动。
> 下面是当时的发现记录。

用测试账号 `mcnai-test-a` 打云端检索（只读），`[private]` 层实际检出：

- **达人信息表的原始表格行**，字段里带**身份证号**（是正文切片，不是 MOC 摘要）
- 2026年收支利润表的表格内容
- 多份年框合作 / 目标管理总表的内容

**要你裁决**：
1. 测试账号 private 层的这批切片要不要清。清要走 Supabase 后台（我没有也不该拿 service-role key）
2. **大头那台**用的是她自己的账号——如果 A-3 之后在登录态跑过入库，同样这四类文件很可能也上去了。这个我这边够不着，需要你从她的账号侧确认

> 注：`[private]` 层有 RLS 隔离，别的用户看不到。但产品在设置页对默认档写的承诺是「敏感文件不离开你的电脑：不发给模型、不上传云端」——这条承诺被破了。

### 🟠 R-03 MOC / 主题索引把敏感文件的摘要带上了云

> **已按方案 B 修掉，见 §6c**（pkb-pipeline `bf07070`）。下面是当时的发现记录。

`_主题索引.md` / `_MOC_工作-管理类.md` **本身不带 `sensitive` 标记**，所以照常上云；而它们的正文逐条摘录了敏感文件的结构化摘要，实测含：

- 真实人名（员工与达人）、**员工职级**（如「员工姓名/黄泓滔/员工职级/P5」）、合同类型（孵化约/正式合作期）、行数与字段名
- 实测 **24 行**引用了人事/财务类敏感文件
- 好消息：MOC 里**没有**身份证号与手机号（0 个）

**这不是 R-01 的漏网，是另一条路**：R-01 修的是"敏感篇自己上云"，这条是"别的篇转述了敏感篇"。

**为什么我没直接修**：三条路都有代价，属产品取舍，该你拍：
1. MOC 引用到敏感文件就整篇标敏感 → 云端检索失去目录层
2. `cloudSync` 上传前剔掉引用敏感文件的行 → 改内容，且云端与本地不一致
3. **（推荐）pipeline 侧 `04_gen_moc` 对敏感文件只写标题、不写字段摘要** → 跨仓库改 pkb-pipeline

### 🟠 R-04 更名连带面

见 §2.3。要决策的是：签名单里要不要把 `productName` 一起改成 SamePage。
按实测，**改它不会导致老用户重登**（§2.3 那张表），代价比 HANDOFF §4-27 记的小。

### 🟠 R-05 离线条不会自己下去

`probeCloud()` 只在两处被调用：应用启动（`did-finish-load` 之后）、登录成功之后。**没有周期性重探。**

所以「早上没网开的应用，中午网回来了」这个形态下，那条写着「云端离线…AI 检索已降级为本地全文」的条子**不会自己消失**——只有等到某次真实云端请求成功（问一次库、入一次库、同步队列到期重试）才会被 `markCloudReachable()` 顺手清掉。用户如果只是在知识库里看笔记，那条过期信息会一直挂着。

修法很小：`probeCloud` 在 `reachable === false` 时挂一个 60s 的重探定时器，探通即清、探通即停。

### ⚪ R-06 `usage-report.mjs` 的「无 route 记录数」报成 6 倍（已修）

`legacyRouteGuesses` 是个自增计数器，而 `recCost()` 会被 5 张分组表 + 总计各调一遍 → 10 条被报成 **60 条**（本机实测）。改成对记录集直接数。这是我这轮对账要用的工具，数字不对会误导判断，所以当场修了。

### ⚪ R-07 走查旧名扫描的两处假红（已修，测试侧）

`assertNoOldBrand` 用 `/mcn[-\s]?ai|拉齐/i` 扫全页可见文本，命中了两类**环境串**：

1. 侧栏身份行的测试账号邮箱 `mcnai-test-a@example.com`
2. 知识库页/换库确认里显示的走查库路径 `/tmp/mcnai-e2e-vault`

两者都是环境与用户数据、不是品牌文案。本地模式验不到（没登录、身份行不显示邮箱），**只有 E2E_CHAT 轮才炸**，而那一轮 ≈¥1.8 —— 靠跑来发现太贵。已改成扫描前先抠掉邮箱与 `/tmp/mcnai-*` 走查目录，**抠的范围刻意收窄**：产品自己生成的名字（建库向导写的欢迎笔记、模板目录名）仍在扫描范围内。

### ⚪ R-08 产物渲染的临时 spec JSON 不清理

见 §2.8。`agent/index.ts` 里 `render_pptx` / `render_document` 写完 spec 就不管了。
建议 `finally` 里 `unlink`，或者干脆改用管道传给渲染子进程。

### 🟠 R-09 生成产物那一轮，摘要说「未找到相关资料」

**现场**：截图 `64b-新装-产物已入库`。第二轮问「把刚才那两句话做成 Word」，
折叠摘要写的是「**核对完成，未找到相关资料 · 用时 4.2s**」，而右边产物面板里
就是刚生成的 `新装验收.docx`，正文也写着「已生成 Word 文档」。

**根因**（`renderer/src/lib/step-stream.ts:290`）：

```ts
export function summaryText(steps, elapsedMs) {
  const n = resourceCount(steps)
  return n > 0 ? `检索了 ${n} 份资料 · 用时 ${secs}` : `核对完成，未找到相关资料 · 用时 ${secs}`
}
```

只有两个分支。而 `resourceCount()` **按设计不把产物步骤算成"资料"**（注释里明写着
「生成产物、写文件这类动作不是"资料"，不计」——这条本身是对的）。两条对的规则撞在一起，
结果就是：**只要这一轮没检索到东西、却生成了产物，摘要一定说"未找到相关资料"**。

**为什么值得修**：这是"追问式产物请求"的常见形状（先问一轮、再让它做成文档），
不是边角。而且它正好踩在 §2-19 规则 10b 立的那条规矩上——
**界面上两处对同一件事的说法不许打架**，用户会怀疑那份文档是编的。

**建议修法**（小、收敛）：`summaryText` 增加产物分支——
这一轮有 `render_pptx`/`render_document` 步骤时说「生成了 Word 文档 · 用时 Xs」
（有检索就并列成「检索了 N 份资料 · 生成了文档」）；
「未找到相关资料」只留给**纯检索/扫描且 0 命中**的轮次。

### 🟠 R-10 升级不会把老用户挪回官方直连（成本 2.7×）

见 §2.2。发版修不了，得在管理员区手动改标准档 baseUrl。
**建议在给大头装新版时顺手做掉**，否则她那台会继续按 2.7 倍的价跑。

### ⚪ R-11 建库引导整屏没有品牌标识

截图 `61-新装-建库向导两分支`：新客户登录后的第一屏是「建立你的知识库」+ 两张卡片，
**整屏没有 logo、没有 SamePage 字标**。登录门有 72px 的 logo，进来反而没有了。
打磨项，加一个小字标即可。

---

## 4. 本轮改动清单

| 文件 | 改动 | 性质 |
|---|---|---|
| `desktop/src/main/lib/sensitive.ts` | 新增：A-8 敏感判据（整块 frontmatter） | 🔴 修复 |
| `desktop/src/main/inbox/orchestrator.ts` | `isSensitive` 改调新模块 | 🔴 修复 |
| `desktop/src/main/smoke-cards.ts` | 新增 7 条 A-8 断言 | 断言 |
| `desktop/scripts/usage-report.mjs` | 修「无 route 记录数」6 倍虚高 | ⚪ 修复 |
| `desktop/e2e/walkthrough.mjs` | 旧名扫描抠掉环境串 | 测试侧 |
| `desktop/src/main/ipc.ts` | 新增走查开关 `MCNAI_E2E_NEW_VAULT` | 测试侧 |
| `desktop/e2e/upgrade-path.mjs` | 新增：升级路径验收 | 测试侧 |
| `desktop/e2e/offline-recovery.mjs` | 新增：离线恢复验收 | 测试侧 |
| `desktop/e2e/fresh-install.mjs` | 新增：全新装机路径验收（含 A-8 / A-4 复验） | 测试侧 |
| `desktop/e2e/probe-name.mjs` | 新增：更名影响面探针（读主进程里的 `app.getName()`） | 测试侧 |

**没有改的**（有意）：R-03 / R-05 / R-08 / R-09 / R-10 / R-11 —— 按「高严重度当场修，
其余列清单给你裁决」的分工，这几条各自带着建议修法在 §3 里，等你拍。

---

## 5. 预算实花

批准 ¥5，实花 **≈ ¥4.4**（在批准额度内，但比我跑前那份 ¥3.6 的估算高）。

| 项 | 轮次 | ¥ | 来源 |
|---|---|---|---|
| `login-provision` | 1 | 0.01 | 估 |
| E2E_CHAT 第 1 轮（登录后第一屏就红） | 0 | ~0.01 | 估 |
| E2E_CHAT 第 2 轮（红在知识库页） | — | **~1.0** | 估（账本被第 3 轮覆盖，量不到） |
| **E2E_CHAT 第 3 轮（跑到终点）** | 10 | **1.91** | 实测（账本重算） |
| `smoke:provider` 最小集 | 4 | **0.58** | 实测 |
| `fresh-install` 第 2 轮（等错选择器） | 0 | ~0.01 | 估 |
| `fresh-install` 第 3 轮 | 8 | ~0.43 | 估（同第 4 轮形状） |
| **`fresh-install` 第 4 轮（全绿）** | 8 | **0.43** | 实测 |
| 主走查（本地 + 打包）/ a1-enqueue / 三个 smoke / upgrade-path / offline-recovery / probe-name | — | **0** | 零调用 |

**两处比预估高，如实说**：

1. **`smoke:provider` 报 ¥0.10、实花 ¥0.58**（5.8 倍）。低估的是 `tools` 那一例——
   它真调了 3 次 `search_knowledge`，上下文比我按"单轮"估的大得多
2. **走查假红重跑吃掉约 ¥1**。两轮都红在同一类东西上：旧名扫描把测试账号邮箱、
   走查库路径当成了旧品牌名（R-07）。**教训与 HANDOFF 记的那条同源**——
   「一轮 E2E_CHAT ≈¥1.8，靠跑来发现断言假设过期太贵」。这次的具体教训是：
   **只有 E2E_CHAT 轮才会登录、才会显示邮箱与库路径，本地模式永远扫不到这两样**，
   所以凡是"只在登录态才出现的界面元素"，断言要在写的时候就想清楚，别等真花钱那轮才炸

**没花的一笔**：增强档（aihubmix / `claude-opus-5`）真实调用未跑，单轮 ≈¥7，见 §1.2。

---

## 6. 发版前建议动作（按顺序）

> 这一节是**第一轮报告时**的建议。两轮裁决之后，前三条都已有结论（见 §6b / §6c），
> 只剩下面这一条还没落地。原文保留在版本历史里，这里只留现状。

1. **大头账号的云端敏感存量怎么处置**（R-02 ②）——唯一还挡着定稿的一条，见 §9
2. 给大头装新版时顺手做 R-10：管理员区把标准档 baseUrl 改成官方直连，省 2.7 倍
3. R-05 / R-08 / R-09 / R-11 / R-12 属发布后队列，修法都写在 §3 / §6b

## 6b. 三条裁决的落地（2026-08-19）

> 编号说明：用户给的 R-1/R-2/R-3 是**按裁决顺序**编的，与本文的 R-01~R-11 不是同一套。
> 对应关系：R-1 = 本文 R-04（更名）；R-2、R-3 是**本文没有的新条目**（霞飞双卡、空库引导）。

### R-1 更名 → 不改，记账

见 §2.3 的「裁决」。`productName` 维持 `mcn-ai`，已写进 HANDOFF §4-27。

### R-2 建卡器旧卡探测（升级库不再双卡）

**实现**（`src/main/vault/entity-cards.ts`）：建卡前扫全库建一张「已有卡」索引
（判据紧：`entity_kind` 命中，或 `doc_type` 等于该类的 docType；名字过与 `ekey` 同一套的归一）。
同名实体在**任意目录**已有卡 → **跳过新建**，改在旧卡上补关联段；**不自动合并**。

**顺序上「标准位置优先」是刻意的**：已按新版建过卡的库行为一个字不变，
否则升级到这一版会让所有卡当场换家。两边都有的库只打 warn、不擅自处置。

**一处我往前推了半步，得说清楚**：只补关联段的话有个你不会想要的连带——
图谱的 `kindOf()` **只认 `entity_kind`**，而被复用的老页大多只有 `doc_type: 达人档案`，
于是 **135 个达人会从陶土色掉回灰色 doc**，上一轮刚调好的角色配色在升级库上等于白做
（走查还查不出来，因为「五种角色都存在」仍然满足）。
所以复用时**只往老页 frontmatter 补 `entity_kind` / `entity_name` 两个字段**——
纯新增，已有字段一个不改不删。这不是合并（没把两页内容并起来），是给一张本来就是
实体页的文件补两个身份字段，可一眼看出、可手工删。

**真实升级库实测**（拿走查库的 md 骨架、删掉 `30_实体/` 还原成"A-3 建卡器还没跑过"的形态）：

| 角色 | 升级前（原库） | 修复后 |
|---|---|---|
| 达人 | 169 | **169**（没退化） |
| 合作方 | 2 | 2 |
| 枢纽 | 7 | 7 |
| doc | 384 | **247** |
| 文件总数 | 562 | 425 |
| **同名实体卡重复** | 霞飞等 **137 对** | **无** |

`doc` 占比 68.3% → 58.1%，仍满足走查那条「多数安静」的硬约束（≥50%）。

> **顺带查实一条**：这个库里重复的不止霞飞——`30_实体/达人` 169 张与
> `20_公司管理/25_达人档案` 158 张里约 **135 对是同一个人**。霞飞只是最显眼的那个。

**走查现场逮到的第二处（我引入的，已修）**：旧卡探测第一版是**单开一遍全库扫描**，
于是每轮 pipeline 把全库 md 读了**两遍**。走查在「投递箱进度条」那步当场红了
——42 秒的等待窗口被压过去，报「没出现进度条」。**同 HANDOFF 记过的那条教训**：
「pipeline 变慢会把一批当时刚好够用的等待上限逐个变成假红」。
修法是把两次扫描合并进 `collect()` 的同一遍，**不是放宽走查的等待上限**——
慢下来是真的，掩盖它才是错的。

**断言**：`npm run smoke:cards` 新增 12 条（零 token），覆盖 跳过新建 / 旧卡补关联段 /
原正文与已有字段不动 / 身份字段只补一次 / 文档侧双链指向旧卡而不是幽灵路径 /
两轮幂等 / 用户改过老卡自动区则进「待合并」。
另加一条**真实升级库**的可选断言：`SMOKE_UPGRADE_VAULT=<path> SMOKE_UPGRADE_LIB=<库目录名> npm run smoke:cards`。

### R-3 空库引导（最小版）

**实现**（`src/renderer/src/pages/Workbench.tsx`）：**有库、但库里一篇都没有**时，
首页副标题从「问你的库，或直接说要做什么」换成「把资料拖进来，或直接问我」，
下方加一行指向（拖进窗口自动入库 / 左侧「个人知识库」用投递箱导入）。
**不做浮层引导**，完整 onboarding 记 roadmap。

**走查现场逮到一处我引入的错**：第一版只判"库里有没有笔记"，于是首跑引导上点了
「暂时跳过」（**一个库都没有**）的那一屏也被换成了新文案——而那时候拖文件进来
`inbox.enqueue` 直接抛（M-06），"拖进来就会入库"是句假话。收紧成先确认 `vaultPath` 存在。

**看截图时又抓到一处自相矛盾（已一并收掉）**：副标题改成「把资料拖进来」之后，
它正下方的输入框 placeholder 还写着「问你的库，或说"把XX做成PPT"…」——
同一屏上两句话打架，正是 R-09 那一类。placeholder 跟着同一个条件换掉了。
**chips 里的「检索我的库」没动**：那是 `config/chips.ts` 的配置项，
空库时该显示什么属于产品决定，留给你定（记 roadmap）。

**断言**：走查新增截图 `02a-工作台-空库引导` ＋ 正反两面——
空库实例断言文案、指向、**输入框 placeholder 不许再说「问你的库」**、
**且不许出现浮层**；主实例（库里几百篇）断言**不许**出现空库引导、
副标题必须还是原来那句（这条比正面更容易坏：`vault.tree()` 读慢一点或抛一下，
用户库里躺着几百篇、首页却挂着"知识库还是空的"）。

## 6c. 第二轮裁决的落地（2026-08-19）

### R-02 云端敏感存量

#### ① 测试账号 `mcnai-test-a` —— ✅ 已清理并验证

用 `SUPABASE_SERVICE_ROLE_KEY`（`webpage/.env.local` 里有）直接删 `knowledge_chunks`，
过滤条件写死 `visibility=private AND owner_user_id=<该账号>`。

| | 删前 | 删后 |
|---|---|---|
| `mcnai-test-a` private 切片 | **3019** | **0** |
| 其它账号 private 切片 | 3581 | **3581**（一条未动） |

**盘点里纠正了一个数**：第一次查报的是「17 条」，那是 PostgREST 默认 1000 行截断造成的假象，
精确计数（`Prefer: count=exact`）才是 3019。

**验证**（按你的判据，用该账号的 token 打产品自己的检索接口）：
四组关键词（达人信息表/收支利润表/目标管理总表/身份证手机号银行卡）
**private 命中 0 条、身份证与手机号模式 0 处**；剩下的命中全是 `org` 层的公开带货教程，与敏感无关。

> **顺带排除了一个更坏的可能**：既然测试账号能检出达人信息表内容，会不会是跨账号可见？
> 查了 `match_knowledge_chunks`（migration 010/012）——private 分支是
> `visibility = 'private' and owner_user_id = p_user_id`，**隔离是对的**，那些切片确实属于测试账号自己。

#### ② 大头账号 `604056836@qq.com` —— ⚠️ 有存量，**只查未动**，等你定处置方式

private 切片 **3581 条 / 444 个文件**。用两个口径交叉筛（本地 `sensitive:true` 路径精确匹配 ＋
**云端切片内容里的 PII 模式**——后者比标签口径硬，因为她机器上的标签状态我够不着）：

**需要处置的 4 个文件 / 398 条切片**

| 切片 | 身份证 | 手机号 | 长数字串 | 文件 |
|---:|---:|---:|---:|---|
| 325 | 0 | **144** | 87 | `80_Library/工作-管理类/业务-内容经纪/OMG美妆x霞飞年框合作.md` |
| 71 | **2** | 0 | 34 | `80_Library/工作-管理类/业务-内容经纪/内容-2026年度目标管理总表.md` |
| 1 | 0 | 0 | 0 | `80_Library/工作-执行类/工作-课程教学类/培训方案与复盘类/课程开发-2026.md` |
| 1 | 0 | 0 | 0 | `80_Library/工作-管理类/业务-签约课程/签约-2026年度目标管理总表.md` |

完整清单（含每文件的 PII 计数）：`/tmp/maggie-cloud-sensitive.json`

**两个口径的差异要说清楚，别被大数字吓到**：按**文件名**跨库匹配会报「91 个文件」，
那是因为 `20_公司管理/25_达人档案/*.md` 那 88 篇在**新 pipeline 产出的库**里被 03b 标了敏感，
而在**她的库**里没有。逐条扫过它们的云端内容：**PII 0 处**（每篇只有 1 条切片，是摘要）。
所以真正带 PII 的就是上表前两个文件。

**裁决（2026-08-19）：后台代清这 4 个 `file_path`**（不走"指导她重新全量同步"那条——
重新同步**清不掉**已有的敏感切片：去重只按 `file_path` 先删后插，而敏感篇修好后根本不会 ingest，
旧切片会原地留着）。

**执行结果**（过滤条件三重：`owner_user_id` + `visibility=private` + **精确 `file_path`**，不用模糊匹配）：

| | 删前 | 删后 |
|---|---|---|
| 大头 private 切片 | 3581 | **3183** |
| 那 4 个文件的切片 | 398 | **0** |
| 其它账号 private | — | **一条未动** |

**验证**（走**产品自己的检索链路**：`match_knowledge_chunks` RPC 传她的 user_id，
等价于她本人在应用里检索；她的账号我没有密码，所以不能像测试账号那样用 token 打 HTTP 接口）：

- 四组关键词（霞飞年框/目标管理总表/收支利润表/达人联系方式）→ **身份证与手机号模式 0 处**
- 对她剩余 3183 条切片做全量 PII 扫描 → **0 处**
- 那 4 个文件的残留切片 → **0 条**

#### 完成判据现状

- 测试账号：✅ 达标
- 大头账号：✅ **PII 判据达标**（0 处）；「关键词查询摸不到敏感内容」这一半有**已接受的残留**，见下

#### ⚠️ 显式接受的残留风险：33 条 MOC 存量切片（2026-08-19 拍板接受）

**这不是"达标"，是记账后接受。** 写在这里是为了它将来能被查到、被重评，而不是被忘掉。

| 项 | 内容 |
|---|---|
| **内容性质** | MOC / 主题索引里的**旧摘要行**：含**真实员工姓名**与**绩效档案的摘要**（形如 `[[…/OMG-企业年终绩效考核评估档案-陈思羽\|…]] — [档案封面]8行·字段:员工姓…`）。是 **R-03 那条泄漏面的存量**——R-03 只让**将来**生成的 MOC 不带摘要，云上已有的旧切片不会自己消失 |
| **数量** | **33 条**（她剩余 65 条 MOC/主题索引切片中的 33 条） |
| **范围** | **仅大头账号自己可查**。`match_knowledge_chunks` 的 private 分支是 `visibility='private' and owner_user_id = p_user_id`（migration 010/012），隔离经查证成立——别的用户、别的组织都摸不到 |
| **不含** | 身份证号、手机号、银行卡号（全量扫描 0 处） |
| **接受理由** | ① **低概率**：只有她本人检索到相关内容时才会出现在结果里；② **可随时清**——一条 DELETE 的事，不需要停机、不需要她配合；③ **下次入库自动重建无摘要版**：R-03 的修复已随冻结产物发出，她升级后跑一次入库，MOC 会重新生成成不带摘要的版本 |
| **触发重评 / 立即清的条件** | ① **给她升级到含 A-8 + R-03 的正式版之后，做一次全量重同步时顺手清掉**（那是最自然的时机，见 `docs/RELEASE.md` §0）；② **任何合规要求出现时立即清**（客户方数据审计、劳动纠纷取证、对外安全问卷等），不必再走一轮讨论 |

**另有一类我刻意没动，别当成漏了**：她账号里 **144 条达人档案切片含真实姓名 + 签约日期 + 账号**。
这批在**她的库**里本来就没标 `sensitive`（旧 pipeline 产出），属于她正常的知识库内容，
删了会打断她自己的检索。这是**标签口径**问题（新 pipeline 的 `03b` 会把这类标成敏感），
该走产品策略而不是后台删数据——**记在这里，不作为本轮的处置项**。

### R-03 MOC 不写敏感篇摘要（方案 B）—— ✅ 已修并冻结

改在 `pkb-pipeline` 的 `04_gen_moc.py`（提交 `bf07070`，冻结产物已拷进
`desktop/resources/pipeline/`，按仓库卫生规则源码同步提交）。

两处都改了：分区 MOC 与主题索引。`sensitive: true` 的笔记**只保留文件名链接、不写摘要**，
顺带修掉摘要为空时留下的悬空「— 」。**链接保留**——目录可达性是 MOC 的全部意义，
而"库里有这么一篇"不算敏感内容。

**验证（dev 形态 + 冻结形态各跑一遍升级库）**：

| 判据 | 改前 | 改后 |
|---|---|---|
| 敏感篇摘要出现在 MOC/主题索引 | **74 条** | **0** |
| 指向敏感篇的链接（目录可达性） | 74 条 | **74 条，全保留** |
| MOC 内身份证/手机号模式 | 0 | **0** |
| 悬空「— 」 | — | **0** |

> **验证器一开始误报了 2 处**，查下来是 `财务报销制度及流程（正文）V2.0.md` 的 `summary`
> 字面就是它自己的文件名，片段匹配撞上了链接文本。改成只比对 `]]` 之后的部分才是准的。
>
> **另记一条**：`mcn-ingest` 二进制的 sha256 **不随阶段脚本变化**（脚本是 `_internal/` 下的
> 数据文件，不进 bootloader），这次冻结后仍是 `c123c6f68dccbb03`。
> 校验阶段脚本改动要比对 `_internal/` 里的 `.py`，**别拿二进制哈希当版本指纹**。

### 这一轮我自己踩的两个坑（记下来，都不是产品 bug）

1. **拷冻结产物时把目录层级搞错，投递箱整条链哑掉**。
   `.gitignore` 里那句说明写的是 `cp -R dist/mcn-ingest resources/pipeline`——**少一个 `/.`**，
   照着敲出来的是 `resources/pipeline/mcn-ingest/`（目录），而 `lib/pipeline.ts` 的
   `pipelineBin()` 要的正是 `resources/pipeline/mcn-ingest` 这个**文件**。
   结果 `spawn` 报 `EACCES`，投递箱一个文件都不处理，**而界面上的表现只是"没有进度条"**
   ——走查在那儿挂了 18 分钟，我一度怀疑是自己新加的 `shell.trashItem` 冻住了主进程。
   真因在 `logs/main.log` 里一行就写着。已修正布局，并把 `.gitignore` 那句说明改准
   （连同这个坑的症状一起写进去了）。
2. **误报追到底才发现是验证器的问题**（见上面 R-03 的注）。

### 一处如实记的验证缺口

存量清理的删除走 `shell.trashItem`（废纸篓），**但真实跑到的是 `fs.rm` 那条兜底**——
`smoke:cards` 在 `ELECTRON_RUN_AS_NODE` 下没有 `shell`，而走查库不构成存量双卡形态、
根本不触发清理。所以**废纸篓那条分支没有被端到端验过**，验过的是"拿不到 Electron 上下文时
退回硬删"。最坏后果是客户机上那张重复卡被硬删而不是进废纸篓——不影响正确性，
但"删错了能捞回来"这个承诺在真机上没验过。

### 存量双卡清理 —— ✅ 已实现并在真实库验证

R-2 只防住了"以后不再产生双卡"，**已经双了的库不会自愈**。这轮按你说的补上：
**旧卡为准 → 关联段并回旧卡 → 标准位置那张删掉**。

**删文件前的放行条件（三条全满足才删）**：有完整自动区、`auto_hash` 与自动区内容对得上
（= 用户没改过）、自动区外面没有用户内容。任何一条不满足就**两张都留着**只报数——
删掉用户写过的东西是这一层绝对不能犯的错（同 M-27「冲突保用户版」的取向）。
删除走**废纸篓**（`shell.trashItem`）不是硬删，与笔记删除同一条口径，删错了能捞回来。

**真实库实测**（把走查库还原成"老页 + 新卡并存"的存量形态再跑）：

| | 清理前 | 清理后 |
|---|---|---|
| 实体页 | 329 | 211 |
| **同名双节点** | **118 组** | **0** |
| 达人 / 合作方 / 枢纽 | 170 / 2 / 7 | **170 / 2 / 7**（无退化） |
| doc 占比 | — | 58.0%（仍满足「多数安静」≥50%） |
| 霞飞落点 | 两处 | 只剩 `20_公司管理/合作方/霞飞.md` |

**断言**：`smoke:cards` 再加 11 条，含**反例**——用户在标准位置那张卡上写过东西时
**不许删**、不计入清理数、用户写的内容原样还在。

## 7. 本轮没覆盖到的（如实记，别当成验过了）

- **增强档真实调用**：¥7/轮，超预算未跑。置灰分支 / 回落逻辑 / 切标准重试出口都只有本地断言
- **`make-ppt` 在增强档上的表现**：HANDOFF「已知未验项」里的老账，本轮仍未验
- **macOS 菜单栏最左那个应用名**：本机没给辅助功能授权，读不到，按 `CFBundleName` 推的，**请人工瞄一眼**
- **查云硬确认**：R-02 是靠云端检索**间接**证实的（检出了内容），没有直接查库列出 `file_path`。
  产品里没有查云入口，也不该为测试新开 IPC —— 同 HANDOFF 那条「网关落地后顺手补」
- **Q4 / Q7 复跑**（实体卡对检索的贡献）：仍然没有任何数据，同 A-3 收官时的状态
- **Windows / 老 macOS 客户机**：本轮全在本机 arm64 上跑，`yara` 预检与老系统兼容性未做

---

## 9. 定稿状态

> ### 定稿之后发生的三件事（2026-08-19 晚，见 `docs/RELEASE.md` 与 HANDOFF §4-2 / §3-13）
>
> 1. **签名与公证已接通**：`.app` 与 `.dmg` 都签名、都公证、都订票；`spctl` 双 accepted。
>    §2.8 那句「本轮全部验证跑在本机 arm64 上」仍然成立，但**「未签名 / 需右键打开」的前提已作废**。
> 2. **Electron 30.5.1 → 43.4.1**（Node 24 / Chromium 150）。本报告里所有"跑在 Electron 30 上"的
>    结论已在 43 上**重跑一遍**：主走查、打包形态走查、E2E_CHAT 全量、a1-enqueue、
>    fresh-install、upgrade-path、三条逻辑冒烟，**全绿**。升级抓到并修掉两条回归，见 HANDOFF §4-2。
> 3. **第一版检索口径改为本地**（§3-13 裁决）：`search_knowledge` 登录态下也走本地全文那一支，
>    云端语义检索本版不启用（开关 `searchBackend`，出厂 `local`，代码不删）。
>    **§1.1 那条「检索策略耗时 128.8s / 达标60秒=false」的软警告，根因正是云端分支不给
>    `file_path`**——本地口径下模型直接拿到路径，不再猜路径、不再补 Grep。
>    **云同步没关**：上云与 embedding 照常，云端 RPC 修好后一键切回，数据是全的。

# ✅ 可发布（2026-08-19 定稿）

发版前自测的全部阻断项已清，三轮裁决全部落地并重验。

| 项 | 结论 |
|---|---|
| **R-01** 敏感文件上云 | ✅ 已修（判据改为整块 frontmatter），647 篇对照零不一致，`smoke:cards` 7 条断言守住 |
| **R-02 ①** 测试账号云端存量 | ✅ 3019 → 0，按判据验证（private 命中 0、PII 0） |
| **R-02 ②** 大头账号云端存量 | ✅ 398 条已清（3581 → 3183），全量 PII 扫描 **0 处**；**另有 33 条 MOC 存量切片作为显式接受的残留风险记账**，见 §6c |
| **R-03** MOC 带敏感摘要 | ✅ 已修（pkb-pipeline `bf07070`），dev 与冻结两种形态各验一遍：泄漏 74 → 0，链接 74 条全保留 |
| **存量双卡** | ✅ 真实库 118 组 → 0，图谱角色无退化（达人 170 / 合作方 2 / doc 58%） |
| **R-04** 更名 | ✅ 维持不改，已记账（§2.3 + HANDOFF §4-27） |
| **全量测试体系** | ✅ 全绿，含**打包形态**；取消与 before-quit 在打包形态验过零孤儿；截图基线干净 |

**发版后第一优先动作**：把大头升级到含 A-8 的版本——**她那台仍是修复前构建，
再触发一次全量同步就会把敏感文件重新推上云**，刚清的 398 条会长回来。
指引见 `docs/RELEASE.md` §0。

发布后队列（不挡这一版）：R-05 / R-08 / R-09 / R-10 / R-11 / R-12，修法都写在 §3 / §6b。

> 打包单另开窗口。
